<div class="booyah-box col-xs-10 col-xs-offset-1">
	<h1>Enter your question details</h1>
	<%= simple_form_for @question do |f| %>
		<%= f.input :question %>
		<br />
		<%= f.submit 'Submit', :class => 'btn btn-primary' %>
	<% end %>
</div>